<div class="container text-center">
  <% if @error %>
    <div class="alert alert-danger" role="alert"><%= @error %></div>
  <% else %>
    <div class="alert alert-info" role="alert">
      To generate PDF click button below. Wait about 10 seconds and you'll get link to the generated PDF. 
      You can also refresh page or visit <%= link_to 'About', about_path, class: 'label label-primary' %> page.
    </div>
    <div class="row">
      <div class="text-center">
        <a class="btn btn-big btn-primary generate-pdf" href="#"><i></i><span>Generate PDF</span></a>
      </div>
    </div>
    <hr>
  <% end %>
  <div class="row">
    <p id="notice" class="alert alert-success" role="alert"><%= notice %></p>
  </div>
</div>

<div class="container">
  <%= render 'shared/content' %>
</div>
